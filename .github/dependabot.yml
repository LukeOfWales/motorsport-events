version: 2
updates:
  # Keep GitHub Actions versions current (would flag action runtime lag, e.g.
  # the Node 20 -> 24 change).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly

  # Python dependencies (requirements*.txt).
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
