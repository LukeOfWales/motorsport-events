[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "motorsport-events"
version = "0.1.0"
description = "Static-site UK motorsport event aggregator (rally, trials, hillclimb, off-road) with distance ranking from a home postcode."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "beautifulsoup4>=4.12.3",
    "lxml>=5.3.0",
    "python-dateutil>=2.9.0",
    "pydantic>=2.11.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "freezegun>=1.5.5",
    "pytest-httpx>=0.36.2",
]

[project.scripts]
mse-build-site = "app.build_site:build"

[tool.setuptools]
packages = ["app", "app.adapters"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-q --cov=app --cov-report=term-missing"

[tool.coverage.run]
omit = [
    "app/build_site.py",
]
[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
]
